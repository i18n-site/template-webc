doctype html
html
  head
    meta(charset="UTF-8")
    meta(content="width=device-width,initial-scale=1" name="viewport")
    for css_url in CSS_URL_LI
      link(href=css_url rel="stylesheet")
    script(type="importmap") !{IMPORT_MAP}
    script.
      API="#{API_URL}"
    block head
  body
    block body
